# CLAUDE.md

> 给 Claude Code 看的项目说明。

## 怎么跟我说话

- 我是做生意的，不是技术人。**别用技术术语**。
- 非用不可的词，先用一句大白话解释它是什么。
- 别拿技术选型来问我（用哪个库、哪个数据库）——你决定，然后一句话告诉我你选了什么、为什么。
- 汇报进度说「现在能做什么了」，不要报改了哪些文件、哪些函数。
- 每做完一步，告诉我：打开哪个网址、点哪里、应该看到什么。

## 这个项目是什么

<!-- 这一节还是空的。 -->

**⚠️ 第一次跟我聊这个项目时，先别写代码。** 按顺序问我下面 4 个问题，**一次只问一个**，等我答完再问下一个：

1. 这个东西是给谁用的？（例如「我公司客服团队 5 个人」）
2. 现在最烦、最浪费时间的是什么事？（例如「客户跟进老是漏掉」）
3. 做出来之后，什么样算成功？（例如「每天早上一眼看到今天该跟进谁」）
4. 他们平时用手机看还是电脑看？

问完把我的回答填进下面的空格里，**删掉上面这段问题和这行注释**，再拿给我确认一眼。以后每次开工都先看这一节。

- **谁在用**：<待填>
- **要解决什么**：<待填>
- **成功长什么样**：<待填>
- **手机还是电脑**：<待填>

## 技术栈

- **Next.js（App Router）** + **TypeScript**
- 样式用 **Tailwind**；组件优先用 `src/components/ui/` 里现成的 shadcn/ui
- **界面 mobile-first responsive web**：先做手机版式，再适配桌面（Tailwind responsive classes）——很多同事用手机看
- 图表用 **recharts**（已装好）
- 数据读写统一走 `src/lib/followups.ts` 里的 `store`
- ⚠️ 这是 **Next.js 16**（较新），有些写法和网上老教程不一样。拿不准时以项目里现有的写法为准。

## 约定

- 一次改一小块，改完让我能验证再继续
- 只碰该碰的文件，别顺手改别的
- 拿不准我要什么，就问我，别猜着往下做
